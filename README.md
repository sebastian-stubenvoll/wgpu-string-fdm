To run the notebook, install the dependencies with `pipenv install` and build the python package with `pipenv run maturin develop`.
Then make sure to select/set up the virtual env python kernel in your notebook.
